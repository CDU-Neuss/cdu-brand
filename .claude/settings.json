{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "command": "npx prettier --write \"$CLAUDE_FILE_PATH\" 2>/dev/null || true"
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "command": "echo \"$CLAUDE_FILE_PATH\" | grep -qE '(pnpm-lock\\.yaml|composer\\.lock|\\.env)' && echo 'BLOCK: Do not edit lock files or .env files' >&2 && exit 2 || exit 0"
      }
    ]
  }
}
